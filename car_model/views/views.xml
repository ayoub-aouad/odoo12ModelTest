<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="car_model.list">
      <field name="name">carModel list</field>
      <field name="model">car_model.car_model</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="car_model.action_window">
      <field name="name">carModel window</field>
      <field name="res_model">car_model.car_model</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="car_model.action_server">
      <field name="name">carModel server</field>
      <field name="model_id" ref="model_car_model_car_model"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="carModel" id="car_model.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="car_model.menu_1" parent="car_model.menu_root"/>
    <menuitem name="Menu 2" id="car_model.menu_2" parent="car_model.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="car_model.menu_1_list" parent="car_model.menu_1"
              action="car_model.action_window"/>
    <menuitem name="Server to list" id="car_model" parent="car_model.menu_2"
              action="car_model.action_server"/>
    -->
  </data>
</odoo>